@model IEnumerable<Project_MVC_CORE_EF.Models.TipoCamera>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoCamera)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoCamera)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoCamera">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoCamera">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoCamera">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
